// Shared data structures for the application

// Navigation item data for sidebar and mobile drawer
export struct NavItemData {
    icon: image,
    text: string,
}

// Settings page state
export struct SettingsState {
    ai-enabled: bool,
    save-audio: bool,
    background-operation: bool,
    dark-theme: bool,
}
